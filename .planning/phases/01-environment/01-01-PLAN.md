---
phase: 01-environment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Evo-RL/scripts/download_weights.sh
autonomous: false
requirements: [ENV-02]

must_haves:
  truths:
    - "Running download_weights.sh downloads all three pretrained weight sets to Evo-RL/pretrained/"
    - "The script supports HF_ENDPOINT override for mirror access on restricted networks"
    - "After download, pi05_base/policy_preprocessor.json references local paligemma tokenizer path"
  artifacts:
    - path: "Evo-RL/scripts/download_weights.sh"
      provides: "One-command weight download with HF mirror support and tokenizer path patching"
      contains: "huggingface-cli download"
  key_links:
    - from: "Evo-RL/scripts/download_weights.sh"
      to: "Evo-RL/pretrained/pi05_base/"
      via: "huggingface-cli download lerobot/pi05_base"
      pattern: "huggingface-cli download lerobot/pi05_base"
    - from: "Evo-RL/scripts/download_weights.sh"
      to: "Evo-RL/pretrained/pi05_base/policy_preprocessor.json"
      via: "sed or python patch for paligemma path"
      pattern: "paligemma"
---

<objective>
Create a robust weight download script that fetches all three missing pretrained models (pi05_base ~14GB, siglip-so400m-patch14-384 ~1.1GB, gemma-3-270m ~0.5GB) and patches the pi05_base tokenizer path to use the local paligemma directory.

Purpose: The three pretrained weights are the primary blocker for Phase 2 training. Without them, no training step can run.
Output: `Evo-RL/scripts/download_weights.sh` — executable, self-contained, supports HF mirror.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-environment/01-RESEARCH.md

Source files for reference:
@Evo-RL/scripts/train_pen_4090.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create download_weights.sh script</name>
  <files>Evo-RL/scripts/download_weights.sh</files>
  <read_first>
    - Evo-RL/scripts/train_pen_4090.sh (lines 60-70 for path conventions)
    - Evo-RL/pretrained/paligemma-3b-pt-224/ (confirm tokenizer files present)
  </read_first>
  <action>
Create `Evo-RL/scripts/download_weights.sh` with the following exact structure:

1. Shebang and `set -euo pipefail`
2. Compute WORK_DIR as parent of script dir: `WORK_DIR="$(cd "$(dirname "$0")/.." && pwd)"`
3. Set `PRETRAINED_DIR="${WORK_DIR}/pretrained"`
4. `mkdir -p "${PRETRAINED_DIR}"`
5. Print header banner showing PRETRAINED_DIR and whether HF_ENDPOINT is set
6. If env var `HF_ENDPOINT` is set, print its value. Add a comment block at top:
   ```
   # Optional: use HF mirror for China network
   #   export HF_ENDPOINT=https://hf-mirror.com
   #   bash scripts/download_weights.sh
   ```
7. Download three models using `huggingface-cli download`:
   - `lerobot/pi05_base` -> `${PRETRAINED_DIR}/pi05_base` (print "Downloading pi05_base (~14GB)...")
   - `google/siglip-so400m-patch14-384` -> `${PRETRAINED_DIR}/siglip-so400m-patch14-384` (print "Downloading siglip-so400m-patch14-384 (~1.1GB)...")
   - `google/gemma-3-270m` -> `${PRETRAINED_DIR}/gemma-3-270m` (print "Downloading gemma-3-270m (~0.5GB)...")
8. After pi05_base download completes, patch `policy_preprocessor.json` to use the local paligemma tokenizer:
   - Read `${PRETRAINED_DIR}/pi05_base/policy_preprocessor.json`
   - Use `python3 -c` inline to load JSON, check if `processor_kwargs.tokenizer_name_or_path` exists, if so replace it with `${PRETRAINED_DIR}/paligemma-3b-pt-224` (absolute path). Also check for `processor_kwargs.tokenizer` as an alternative key. Write back.
   - Print what was patched or "no patch needed" if field not found
   - Rationale comment: "pi05_base defaults to HF repo ID 'google/paligemma-3b-pt-224' which fails offline; patch to local path"
9. Final summary: print all three weight dirs and whether they exist with `ls -ld`
10. Make the script executable: `chmod +x`

Do NOT use wget/curl for downloads — `huggingface-cli download` handles partial downloads, auth, and file structure.
Do NOT hardcode absolute paths to `/home/wzt` — use WORK_DIR-relative paths for pretrained dir, but the patched paligemma path must be absolute (computed at runtime).
  </action>
  <verify>
    <automated>bash -n /home/wzt/wzt/mycode/my_RL/Evo-RL/scripts/download_weights.sh && echo "SYNTAX OK" && grep -c "huggingface-cli download" /home/wzt/wzt/mycode/my_RL/Evo-RL/scripts/download_weights.sh | grep -q "3" && echo "3 DOWNLOADS OK" && grep -q "policy_preprocessor.json" /home/wzt/wzt/mycode/my_RL/Evo-RL/scripts/download_weights.sh && echo "PATCH OK" && grep -q "HF_ENDPOINT" /home/wzt/wzt/mycode/my_RL/Evo-RL/scripts/download_weights.sh && echo "MIRROR OK"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `Evo-RL/scripts/download_weights.sh` and is executable (`-x` permission)
    - `bash -n` syntax check passes with no errors
    - Contains exactly 3 `huggingface-cli download` commands (one per model)
    - Contains `--local-dir` for each download pointing to `${PRETRAINED_DIR}/<model_name>`
    - Contains `HF_ENDPOINT` reference (mirror support)
    - Contains `policy_preprocessor.json` patching logic
    - Contains `paligemma-3b-pt-224` reference in patching section
    - Uses `set -euo pipefail`
    - Does NOT hardcode `/home/wzt` anywhere
  </acceptance_criteria>
  <done>download_weights.sh created, syntax-valid, contains all 3 downloads + tokenizer patch + HF mirror support, no hardcoded user paths</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User runs download and verifies weights</name>
  <files>Evo-RL/pretrained/pi05_base, Evo-RL/pretrained/siglip-so400m-patch14-384, Evo-RL/pretrained/gemma-3-270m</files>
  <read_first>
    - Evo-RL/scripts/download_weights.sh (the script user will run)
  </read_first>
  <action>
This is a human verification checkpoint. The user must run the download script manually because:
1. Downloads total ~15GB and take significant time
2. Network access may require HF_ENDPOINT mirror configuration
3. The user must confirm weights are on disk before Phase 2 can proceed

Present these instructions to the user:
1. Activate conda env: `conda activate evo-rl`
2. cd to project root: `cd /home/wzt/wzt/mycode/my_RL/Evo-RL`
3. Run: `bash scripts/download_weights.sh`
   (If behind firewall: `HF_ENDPOINT=https://hf-mirror.com bash scripts/download_weights.sh`)
4. Wait for completion (~15GB total)
5. Verify: `ls -ld pretrained/pi05_base/ pretrained/siglip-so400m-patch14-384/ pretrained/gemma-3-270m/`
6. Verify tokenizer patch: `python3 -c "import json; print(json.load(open('pretrained/pi05_base/policy_preprocessor.json')))" | grep paligemma`
  </action>
  <verify>
    <automated>ls -d /home/wzt/wzt/mycode/my_RL/Evo-RL/pretrained/pi05_base /home/wzt/wzt/mycode/my_RL/Evo-RL/pretrained/siglip-so400m-patch14-384 /home/wzt/wzt/mycode/my_RL/Evo-RL/pretrained/gemma-3-270m 2>/dev/null && echo "ALL WEIGHTS PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - Directory `Evo-RL/pretrained/pi05_base/` exists and contains model files
    - Directory `Evo-RL/pretrained/siglip-so400m-patch14-384/` exists and contains model files
    - Directory `Evo-RL/pretrained/gemma-3-270m/` exists and contains model files
    - `pi05_base/policy_preprocessor.json` contains local paligemma path (not google/paligemma-3b-pt-224 HF ID)
  </acceptance_criteria>
  <done>All three pretrained weight directories populated, tokenizer path patched to local</done>
</task>

</tasks>

<verification>
- `bash -n Evo-RL/scripts/download_weights.sh` passes (syntax valid)
- Script contains exactly 3 `huggingface-cli download` calls
- Script references `HF_ENDPOINT` for mirror support
- Script patches `policy_preprocessor.json` after pi05_base download
- After user runs: all 3 weight directories populated under `Evo-RL/pretrained/`
</verification>

<success_criteria>
All three pretrained weight directories exist and contain model files:
- `Evo-RL/pretrained/pi05_base/` (~14GB)
- `Evo-RL/pretrained/siglip-so400m-patch14-384/` (~1.1GB)
- `Evo-RL/pretrained/gemma-3-270m/` (~0.5GB)
And `pi05_base/policy_preprocessor.json` references local paligemma tokenizer path.
</success_criteria>

<output>
After completion, create `.planning/phases/01-environment/01-01-SUMMARY.md`
</output>
